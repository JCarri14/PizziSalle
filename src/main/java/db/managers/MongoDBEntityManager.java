package db.managers;

import db.interfaces.DBEntityManager;

public abstract class MongoDBEntityManager<Param> implements DBEntityManager<Param> {
    // Possible variables needed for querying to database
}
